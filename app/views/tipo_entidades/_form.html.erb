<%= form_for(tipo_entidade) do |f| %>
  <% if tipo_entidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_entidade.errors.count, "error") %> prohibited this tipo_entidade from being saved:</h2>

      <ul>
      <% tipo_entidade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_field :descricao %>
  </div>
    <div class="field">
    <%= f.label :codigo %>
    <%= f.text_field :codigo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
